//LINK API functions for creating water marked images.

create a trigger with a name createTrigger(name)
create a payoff with a name and url createPayoff(name, url)
create link with a link name  createLink()

upload a jpeg using submit

get the uploaded file getUploadedFile()
which will be used to create an event.
